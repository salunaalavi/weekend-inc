import styles from "./styles.module.scss";

const Footer = (props) => (
    <footer {...props}>
        <div className={styles.footer}>
            Ini Footer
        </div>
    </footer>
);

export default Footer;